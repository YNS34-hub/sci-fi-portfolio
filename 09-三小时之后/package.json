{
  "name": "after-three-hours",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4411",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4411 --strictPort",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:static": "node scripts/check-static.mjs",
    "test:e2e": "playwright test",
    "test:delivery": "node scripts/verify-delivery.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.54.2",
    "@vitest/coverage-v8": "^3.2.4",
    "vite": "^7.1.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
